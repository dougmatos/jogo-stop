# Stop — Sorteador de Letras: Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Criar um app Angular 17+ com tela inicial e tela de jogo onde o usuário sorteia letras A–Z via animação de slot machine com efeitos sonoros procedurais.

**Architecture:** Componentes standalone com Angular Signals para estado reativo sem RxJS. `GameService` centraliza o estado do jogo (letras disponíveis, sorteadas, fase). `RouletteComponent` executa a animação via Web Animations API (3 fases: aceleração → rolagem → desaceleração) e `AudioService` gera sons proceduralmente com `AudioContext`.

**Tech Stack:** Angular 17+, Web Animations API, Web Audio API (`AudioContext`), SVG inline, CSS custom properties (tema neon/dark), Jasmine/Karma para testes.

---

## Mapa de Arquivos

| Arquivo | Ação | Responsabilidade |
|---|---|---|
| `src/styles.css` | Modificar | CSS custom properties do tema neon/dark |
| `src/app/app.component.ts` | Modificar | Signal `view`, alterna home ↔ game |
| `src/app/app.component.html` | Modificar | `@if` switching entre HomeComponent e GameComponent |
| `src/app/app.component.css` | Modificar | Layout raiz (full-height, fundo) |
| `src/app/services/game.service.ts` | Criar | Estado: signals + drawLetter/resetGame/onAnimationComplete |
| `src/app/services/game.service.spec.ts` | Criar | Testes unitários do GameService |
| `src/app/services/audio.service.ts` | Criar | AudioContext: tick/ding/swoosh procedurais |
| `src/app/services/audio.service.spec.ts` | Criar | Testes de smoke do AudioService |
| `src/app/components/alphabet-grid/alphabet-grid.component.ts` | Criar | Computed de 26 letras com flag `drawn` |
| `src/app/components/alphabet-grid/alphabet-grid.component.html` | Criar | Grid 9×3 com `@for` |
| `src/app/components/alphabet-grid/alphabet-grid.component.css` | Criar | Estilo neon das células |
| `src/app/components/alphabet-grid/alphabet-grid.component.spec.ts` | Criar | Testes de renderização da grade |
| `src/app/components/home/home.component.ts` | Criar | Output `startGame`, chama resetGame + playSwoosh |
| `src/app/components/home/home.component.html` | Criar | Logo SVG animado + botão NOVO JOGO |
| `src/app/components/home/home.component.css` | Criar | Layout centralizado, animação do logo |
| `src/app/components/roulette/roulette.component.ts` | Criar | Inputs phase/currentLetter, efeito de animação 3 fases |
| `src/app/components/roulette/roulette.component.html` | Criar | SVG do tambor + janela com mask |
| `src/app/components/roulette/roulette.component.css` | Criar | Janela, drum, slots, linha central, glow |
| `src/app/components/game/game.component.ts` | Criar | Injeta GameService, expõe signals para o template |
| `src/app/components/game/game.component.html` | Criar | Tambor + botão Sortear + grade + botão Novo Jogo |
| `src/app/components/game/game.component.css` | Criar | Layout em coluna centralizado |

---

## Task 1: Bootstrap do Projeto Angular

**Files:**
- Scaffold em: `/c/Users/Douglas/source/pessoais/jogo-stop/`

- [ ] **Step 1: Gerar o projeto Angular**

```bash
cd /c/Users/Douglas/source/pessoais/jogo-stop
npx @angular/cli@latest new jogo-stop --directory . --style=css --routing=false --skip-git --inline-template=false
```

Quando perguntado sobre SSR, responder **N**.

- [ ] **Step 2: Verificar que o app sobe**

```bash
cd /c/Users/Douglas/source/pessoais/jogo-stop
npx ng serve --open
```

Esperado: browser abre em `http://localhost:4200` com a tela padrão do Angular.
Encerrar com `Ctrl+C`.

- [ ] **Step 3: Verificar que os testes passam**

```bash
npx ng test --watch=false --browsers=ChromeHeadless
```

Esperado: `Executed N specs, 0 failures`.

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "feat: scaffold Angular 17+ project

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"
```

---

## Task 2: Tema Global Neon/Dark

**Files:**
- Modify: `src/styles.css`
- Modify: `src/app/app.component.css`
- Modify: `src/app/app.component.html`
- Modify: `src/app/app.component.ts`

- [ ] **Step 1: Substituir `src/styles.css` pelo tema neon/dark**

```css
/* src/styles.css */
*, *::before, *::after {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

:root {
  --bg-primary:      #0d0d1a;
  --bg-secondary:    #080814;
  --neon-cyan:       #00f5ff;
  --neon-cyan-dim:   #00f5ff44;
  --neon-cyan-bg:    #00f5ff0a;
  --neon-purple:     #bf00ff;
  --neon-purple-dim: #bf00ff44;
  --text-muted:      rgba(255,255,255,0.13);
  --glow-cyan:       0 0 12px #00f5ff88;
  --glow-purple:     0 0 10px #bf00ff88;
}

html, body {
  height: 100%;
  background: var(--bg-primary);
  color: white;
  font-family: monospace, monospace;
}
```

- [ ] **Step 2: Atualizar `src/app/app.component.css`**

```css
/* src/app/app.component.css */
:host {
  display: flex;
  align-items: center;
  justify-content: center;
  min-height: 100vh;
  background: var(--bg-primary);
}
```

- [ ] **Step 3: Simplificar `src/app/app.component.html` temporariamente**

```html
<p style="color: var(--neon-cyan); font-family: monospace;">STOP — tema ok</p>
```

- [ ] **Step 4: Atualizar `src/app/app.component.ts` para standalone mínimo**

```typescript
import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  standalone: true,
  imports: [],
  templateUrl: './app.component.html',
  styleUrl: './app.component.css'
})
export class AppComponent {}
```

- [ ] **Step 5: Confirmar visualmente**

```bash
npx ng serve --open
```

Esperado: página escura com texto "STOP — tema ok" em ciano.
Encerrar com `Ctrl+C`.

- [ ] **Step 6: Commit**

```bash
git add src/styles.css src/app/app.component.ts src/app/app.component.html src/app/app.component.css
git commit -m "feat: add neon/dark theme CSS variables

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"
```

---

## Task 3: GameService (TDD)

**Files:**
- Create: `src/app/services/game.service.ts`
- Create: `src/app/services/game.service.spec.ts`

- [ ] **Step 1: Criar o arquivo de teste**

```typescript
// src/app/services/game.service.spec.ts
import { TestBed, fakeAsync, tick } from '@angular/core/testing';
import { GameService } from './game.service';

describe('GameService', () => {
  let service: GameService;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(GameService);
  });

  it('should start with 26 available letters', () => {
    expect(service.availableLetters().length).toBe(26);
  });

  it('should start with no drawn letters', () => {
    expect(service.drawnLetters().length).toBe(0);
  });

  it('should start with null currentLetter', () => {
    expect(service.currentLetter()).toBeNull();
  });

  it('should start in idle phase', () => {
    expect(service.phase()).toBe('idle');
  });

  it('drawLetter should reduce availableLetters by 1', () => {
    service.drawLetter();
    expect(service.availableLetters().length).toBe(25);
  });

  it('drawLetter should add one letter to drawnLetters', () => {
    service.drawLetter();
    expect(service.drawnLetters().length).toBe(1);
  });

  it('drawLetter should set currentLetter to a non-null value', () => {
    service.drawLetter();
    expect(service.currentLetter()).not.toBeNull();
  });

  it('drawn letter should not remain in availableLetters', () => {
    service.drawLetter();
    const drawn = service.currentLetter()!;
    expect(service.availableLetters()).not.toContain(drawn);
  });

  it('drawLetter should set phase to spinning', () => {
    service.drawLetter();
    expect(service.phase()).toBe('spinning');
  });

  it('drawLetter should be a no-op when phase is spinning', () => {
    service.drawLetter();
    service.drawLetter(); // second call while spinning
    expect(service.availableLetters().length).toBe(25);
  });

  it('onAnimationComplete should set phase to result', () => {
    service.drawLetter();
    service.onAnimationComplete();
    expect(service.phase()).toBe('result');
  });

  it('onAnimationComplete should set phase to idle after 1500ms', fakeAsync(() => {
    service.drawLetter();
    service.onAnimationComplete();
    tick(1500);
    expect(service.phase()).toBe('idle');
  }));

  it('resetGame should restore 26 available letters', () => {
    service.drawLetter();
    service.resetGame();
    expect(service.availableLetters().length).toBe(26);
  });

  it('resetGame should clear drawnLetters', () => {
    service.drawLetter();
    service.resetGame();
    expect(service.drawnLetters().length).toBe(0);
  });

  it('resetGame should set currentLetter to null', () => {
    service.drawLetter();
    service.resetGame();
    expect(service.currentLetter()).toBeNull();
  });

  it('resetGame should set phase to idle', () => {
    service.drawLetter();
    service.onAnimationComplete();
    service.resetGame();
    expect(service.phase()).toBe('idle');
  });

  it('resetGame should cancel the result timer', fakeAsync(() => {
    service.drawLetter();
    service.onAnimationComplete();
    service.resetGame();
    tick(1500); // timer should be cleared, no error
    expect(service.phase()).toBe('idle');
  }));
});
```

- [ ] **Step 2: Rodar os testes para confirmar que falham**

```bash
npx ng test --watch=false --browsers=ChromeHeadless --include="**/game.service.spec.ts"
```

Esperado: falha com `Cannot find module './game.service'` ou `GameService is not defined`.

- [ ] **Step 3: Criar a implementação do GameService**

```typescript
// src/app/services/game.service.ts
import { Injectable, signal } from '@angular/core';

export type GamePhase = 'idle' | 'spinning' | 'result';

const ALL_LETTERS = 'ABCDEFGHIJKLMNOPQRSTUVWXYZ'.split('');

@Injectable({ providedIn: 'root' })
export class GameService {
  private _availableLetters = signal<string[]>([...ALL_LETTERS]);
  private _drawnLetters     = signal<string[]>([]);
  private _currentLetter    = signal<string | null>(null);
  private _phase            = signal<GamePhase>('idle');

  readonly availableLetters = this._availableLetters.asReadonly();
  readonly drawnLetters     = this._drawnLetters.asReadonly();
  readonly currentLetter    = this._currentLetter.asReadonly();
  readonly phase            = this._phase.asReadonly();

  private resultTimer: ReturnType<typeof setTimeout> | null = null;

  drawLetter(): void {
    const available = this._availableLetters();
    if (available.length === 0 || this._phase() !== 'idle') return;

    const idx    = Math.floor(Math.random() * available.length);
    const letter = available[idx];

    this._availableLetters.set(available.filter((_, i) => i !== idx));
    this._drawnLetters.update(drawn => [...drawn, letter]);
    this._currentLetter.set(letter);
    this._phase.set('spinning');
  }

  onAnimationComplete(): void {
    if (this._phase() !== 'spinning') return;
    this._phase.set('result');
    this.resultTimer = setTimeout(() => {
      this._phase.set('idle');
      this.resultTimer = null;
    }, 1500);
  }

  resetGame(): void {
    if (this.resultTimer !== null) {
      clearTimeout(this.resultTimer);
      this.resultTimer = null;
    }
    this._availableLetters.set([...ALL_LETTERS]);
    this._drawnLetters.set([]);
    this._currentLetter.set(null);
    this._phase.set('idle');
  }
}
```

- [ ] **Step 4: Rodar os testes para confirmar que passam**

```bash
npx ng test --watch=false --browsers=ChromeHeadless --include="**/game.service.spec.ts"
```

Esperado: `Executed 17 specs, 0 failures`.

- [ ] **Step 5: Commit**

```bash
git add src/app/services/game.service.ts src/app/services/game.service.spec.ts
git commit -m "feat: add GameService with signals and unit tests

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"
```

---

## Task 4: AudioService

**Files:**
- Create: `src/app/services/audio.service.ts`
- Create: `src/app/services/audio.service.spec.ts`

- [ ] **Step 1: Criar o arquivo de teste (smoke test)**

```typescript
// src/app/services/audio.service.spec.ts
import { TestBed } from '@angular/core/testing';
import { AudioService } from './audio.service';

describe('AudioService', () => {
  let service: AudioService;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(AudioService);
  });

  it('should be created', () => {
    expect(service).toBeTruthy();
  });

  it('ensureContext should not throw even without user gesture', () => {
    expect(() => service.ensureContext()).not.toThrow();
  });

  it('playTick should not throw when context is not ready', () => {
    expect(() => service.playTick()).not.toThrow();
  });

  it('playDing should not throw when context is not ready', () => {
    expect(() => service.playDing()).not.toThrow();
  });

  it('playSwoosh should not throw when context is not ready', () => {
    expect(() => service.playSwoosh()).not.toThrow();
  });
});
```

- [ ] **Step 2: Rodar testes para confirmar que falham**

```bash
npx ng test --watch=false --browsers=ChromeHeadless --include="**/audio.service.spec.ts"
```

Esperado: falha com `Cannot find module './audio.service'`.

- [ ] **Step 3: Criar a implementação do AudioService**

```typescript
// src/app/services/audio.service.ts
import { Injectable } from '@angular/core';

@Injectable({ providedIn: 'root' })
export class AudioService {
  private ctx: AudioContext | null = null;

  ensureContext(): void {
    try {
      if (!this.ctx) {
        this.ctx = new AudioContext();
      }
      if (this.ctx.state === 'suspended') {
        this.ctx.resume();
      }
    } catch {
      // Browser sem suporte a AudioContext — modo silencioso
    }
  }

  playTick(): void {
    if (!this.ctx) return;
    try {
      const osc  = this.ctx.createOscillator();
      const gain = this.ctx.createGain();
      osc.connect(gain);
      gain.connect(this.ctx.destination);
      osc.type = 'square';
      osc.frequency.setValueAtTime(220, this.ctx.currentTime);
      gain.gain.setValueAtTime(0.12, this.ctx.currentTime);
      gain.gain.exponentialRampToValueAtTime(0.001, this.ctx.currentTime + 0.04);
      osc.start(this.ctx.currentTime);
      osc.stop(this.ctx.currentTime + 0.04);
    } catch { /* silent */ }
  }

  playDing(): void {
    if (!this.ctx) return;
    try {
      const osc  = this.ctx.createOscillator();
      const gain = this.ctx.createGain();
      osc.connect(gain);
      gain.connect(this.ctx.destination);
      osc.type = 'sine';
      osc.frequency.setValueAtTime(880, this.ctx.currentTime);
      gain.gain.setValueAtTime(0.35, this.ctx.currentTime);
      gain.gain.exponentialRampToValueAtTime(0.001, this.ctx.currentTime + 0.3);
      osc.start(this.ctx.currentTime);
      osc.stop(this.ctx.currentTime + 0.3);
    } catch { /* silent */ }
  }

  playSwoosh(): void {
    if (!this.ctx) return;
    try {
      const bufferSize = Math.floor(this.ctx.sampleRate * 0.15);
      const buffer     = this.ctx.createBuffer(1, bufferSize, this.ctx.sampleRate);
      const data       = buffer.getChannelData(0);
      for (let i = 0; i < bufferSize; i++) {
        data[i] = (Math.random() * 2 - 1) * (1 - i / bufferSize);
      }
      const source = this.ctx.createBufferSource();
      const filter = this.ctx.createBiquadFilter();
      filter.type = 'lowpass';
      filter.frequency.setValueAtTime(800, this.ctx.currentTime);
      source.buffer = buffer;
      source.connect(filter);
      filter.connect(this.ctx.destination);
      source.start();
    } catch { /* silent */ }
  }
}
```

- [ ] **Step 4: Rodar testes**

```bash
npx ng test --watch=false --browsers=ChromeHeadless --include="**/audio.service.spec.ts"
```

Esperado: `Executed 5 specs, 0 failures`.

- [ ] **Step 5: Commit**

```bash
git add src/app/services/audio.service.ts src/app/services/audio.service.spec.ts
git commit -m "feat: add AudioService with procedural tick/ding/swoosh sounds

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"
```

---

## Task 5: AlphabetGridComponent (TDD)

**Files:**
- Create: `src/app/components/alphabet-grid/alphabet-grid.component.ts`
- Create: `src/app/components/alphabet-grid/alphabet-grid.component.html`
- Create: `src/app/components/alphabet-grid/alphabet-grid.component.css`
- Create: `src/app/components/alphabet-grid/alphabet-grid.component.spec.ts`

- [ ] **Step 1: Criar a estrutura de diretório e o arquivo de teste**

```bash
mkdir -p /c/Users/Douglas/source/pessoais/jogo-stop/src/app/components/alphabet-grid
```

```typescript
// src/app/components/alphabet-grid/alphabet-grid.component.spec.ts
import { ComponentFixture, TestBed } from '@angular/core/testing';
import { AlphabetGridComponent } from './alphabet-grid.component';

describe('AlphabetGridComponent', () => {
  let component: AlphabetGridComponent;
  let fixture: ComponentFixture<AlphabetGridComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [AlphabetGridComponent]
    }).compileComponents();

    fixture = TestBed.createComponent(AlphabetGridComponent);
    component = fixture.componentInstance;
    fixture.componentRef.setInput('drawnLetters', []);
    fixture.detectChanges();
  });

  it('should render exactly 26 letter cells', () => {
    const cells = fixture.nativeElement.querySelectorAll('.letter-cell');
    expect(cells.length).toBe(26);
  });

  it('should render all letters A through Z', () => {
    const cells: NodeListOf<HTMLElement> = fixture.nativeElement.querySelectorAll('.letter-cell');
    const rendered = Array.from(cells).map(el => el.textContent!.trim());
    const expected = 'ABCDEFGHIJKLMNOPQRSTUVWXYZ'.split('');
    expect(rendered).toEqual(expected);
  });

  it('should apply "drawn" class to drawn letters', () => {
    fixture.componentRef.setInput('drawnLetters', ['A', 'Z']);
    fixture.detectChanges();
    const drawnCells = fixture.nativeElement.querySelectorAll('.letter-cell.drawn');
    expect(drawnCells.length).toBe(2);
  });

  it('should not apply "drawn" class to available letters', () => {
    fixture.componentRef.setInput('drawnLetters', ['A']);
    fixture.detectChanges();
    const availCells = fixture.nativeElement.querySelectorAll('.letter-cell:not(.drawn)');
    expect(availCells.length).toBe(25);
  });

  it('should update drawn class when drawnLetters input changes', () => {
    fixture.componentRef.setInput('drawnLetters', ['A', 'B', 'C']);
    fixture.detectChanges();
    const drawnCells = fixture.nativeElement.querySelectorAll('.letter-cell.drawn');
    expect(drawnCells.length).toBe(3);
  });
});
```

- [ ] **Step 2: Rodar testes para confirmar que falham**

```bash
npx ng test --watch=false --browsers=ChromeHeadless --include="**/alphabet-grid.component.spec.ts"
```

Esperado: falha com `Cannot find module './alphabet-grid.component'`.

- [ ] **Step 3: Criar o componente TypeScript**

```typescript
// src/app/components/alphabet-grid/alphabet-grid.component.ts
import { Component, input, computed } from '@angular/core';

const ALL_LETTERS = 'ABCDEFGHIJKLMNOPQRSTUVWXYZ'.split('');

@Component({
  selector: 'app-alphabet-grid',
  standalone: true,
  imports: [],
  templateUrl: './alphabet-grid.component.html',
  styleUrl: './alphabet-grid.component.css'
})
export class AlphabetGridComponent {
  drawnLetters = input<string[]>([]);

  letters = computed(() => {
    const drawnSet = new Set(this.drawnLetters());
    return ALL_LETTERS.map(letter => ({
      letter,
      drawn: drawnSet.has(letter)
    }));
  });
}
```

- [ ] **Step 4: Criar o template HTML**

```html
<!-- src/app/components/alphabet-grid/alphabet-grid.component.html -->
<div class="grid">
  @for (item of letters(); track item.letter) {
    <div class="letter-cell" [class.drawn]="item.drawn">
      {{ item.letter }}
    </div>
  }
</div>
```

- [ ] **Step 5: Criar o CSS**

```css
/* src/app/components/alphabet-grid/alphabet-grid.component.css */
.grid {
  display: grid;
  grid-template-columns: repeat(9, 1fr);
  gap: 6px;
  width: 100%;
  max-width: 320px;
}

.letter-cell {
  height: 30px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 4px;
  font-family: monospace;
  font-size: 13px;
  font-weight: bold;
  border: 1px solid var(--neon-cyan-dim);
  color: var(--neon-cyan);
  background: var(--neon-cyan-bg);
  transition: color 0.4s ease, border-color 0.4s ease, background 0.4s ease, text-decoration 0.4s ease;
  letter-spacing: 1px;
}

.letter-cell.drawn {
  color: var(--text-muted);
  text-decoration: line-through;
  border-color: var(--text-muted);
  background: transparent;
}
```

- [ ] **Step 6: Rodar testes**

```bash
npx ng test --watch=false --browsers=ChromeHeadless --include="**/alphabet-grid.component.spec.ts"
```

Esperado: `Executed 5 specs, 0 failures`.

- [ ] **Step 7: Commit**

```bash
git add src/app/components/alphabet-grid/
git commit -m "feat: add AlphabetGridComponent with drawn/available states

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"
```

---

## Task 6: AppComponent — Shell de Navegação

**Files:**
- Modify: `src/app/app.component.ts`
- Modify: `src/app/app.component.html`
- Modify: `src/app/app.component.css`

> Nesta task o HomeComponent e GameComponent ainda não existem. Criamos stubs para o AppComponent poder compilar.

- [ ] **Step 1: Criar diretórios dos componentes filhos**

```bash
mkdir -p /c/Users/Douglas/source/pessoais/jogo-stop/src/app/components/home
mkdir -p /c/Users/Douglas/source/pessoais/jogo-stop/src/app/components/game
mkdir -p /c/Users/Douglas/source/pessoais/jogo-stop/src/app/components/roulette
```

- [ ] **Step 2: Criar stub do HomeComponent**

```typescript
// src/app/components/home/home.component.ts
import { Component, output } from '@angular/core';

@Component({
  selector: 'app-home',
  standalone: true,
  imports: [],
  template: `<p style="color:var(--neon-cyan)">HOME STUB</p>`
})
export class HomeComponent {
  startGame = output<void>();
}
```

- [ ] **Step 3: Criar stub do GameComponent**

```typescript
// src/app/components/game/game.component.ts
import { Component, output } from '@angular/core';

@Component({
  selector: 'app-game',
  standalone: true,
  imports: [],
  template: `<p style="color:var(--neon-purple)">GAME STUB</p>`
})
export class GameComponent {
  goHome = output<void>();
}
```

- [ ] **Step 4: Atualizar AppComponent**

```typescript
// src/app/app.component.ts
import { Component, signal } from '@angular/core';
import { HomeComponent } from './components/home/home.component';
import { GameComponent } from './components/game/game.component';

@Component({
  selector: 'app-root',
  standalone: true,
  imports: [HomeComponent, GameComponent],
  templateUrl: './app.component.html',
  styleUrl: './app.component.css'
})
export class AppComponent {
  view = signal<'home' | 'game'>('home');

  showGame(): void {
    this.view.set('game');
  }

  showHome(): void {
    this.view.set('home');
  }
}
```

- [ ] **Step 5: Atualizar template do AppComponent**

```html
<!-- src/app/app.component.html -->
@if (view() === 'home') {
  <app-home (startGame)="showGame()" />
} @else {
  <app-game (goHome)="showHome()" />
}
```

- [ ] **Step 6: Verificar que compila e os stubs aparecem**

```bash
npx ng serve --open
```

Esperado: "HOME STUB" em ciano. Encerrar com `Ctrl+C`.

- [ ] **Step 7: Commit**

```bash
git add src/app/app.component.ts src/app/app.component.html src/app/app.component.css \
        src/app/components/home/home.component.ts \
        src/app/components/game/game.component.ts
git commit -m "feat: add AppComponent navigation shell with home/game stubs

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"
```

---

## Task 7: HomeComponent

**Files:**
- Modify: `src/app/components/home/home.component.ts`
- Create: `src/app/components/home/home.component.html`
- Create: `src/app/components/home/home.component.css`

- [ ] **Step 1: Atualizar o TypeScript do HomeComponent**

```typescript
// src/app/components/home/home.component.ts
import { Component, inject, output, computed } from '@angular/core';
import { GameService } from '../../services/game.service';
import { AudioService } from '../../services/audio.service';

@Component({
  selector: 'app-home',
  standalone: true,
  imports: [],
  templateUrl: './home.component.html',
  styleUrl: './home.component.css'
})
export class HomeComponent {
  startGame = output<void>();

  private gameService = inject(GameService);
  private audioService = inject(AudioService);

  availableCount = computed(() => this.gameService.availableLetters().length);

  onNewGame(): void {
    this.audioService.ensureContext();
    this.gameService.resetGame();
    this.audioService.playSwoosh();
    this.startGame.emit();
  }
}
```

- [ ] **Step 2: Criar o template HTML com logo SVG**

```html
<!-- src/app/components/home/home.component.html -->
<div class="home">
  <div class="logo-wrapper">
    <svg class="logo" width="130" height="130" viewBox="0 0 130 130" xmlns="http://www.w3.org/2000/svg">
      <defs>
        <filter id="glow-logo">
          <feGaussianBlur stdDeviation="4" result="blur"/>
          <feMerge>
            <feMergeNode in="blur"/>
            <feMergeNode in="SourceGraphic"/>
          </feMerge>
        </filter>
      </defs>
      <!-- Anel externo tracejado (gira com CSS) -->
      <circle class="outer-ring" cx="65" cy="65" r="60"
              fill="none" stroke="#00f5ff" stroke-width="1.5"
              stroke-dasharray="8 4" opacity="0.4"/>
      <!-- Anel interno com brilho -->
      <circle cx="65" cy="65" r="48"
              fill="#080814" stroke="#bf00ff" stroke-width="2"
              filter="url(#glow-logo)"/>
      <!-- Texto central -->
      <text x="65" y="76" text-anchor="middle"
            font-size="40" font-weight="bold"
            fill="#00f5ff" font-family="monospace"
            filter="url(#glow-logo)">S!</text>
    </svg>
  </div>

  <div class="title-block">
    <h1 class="title">STOP</h1>
    <p class="subtitle">SORTEADOR DE LETRAS</p>
  </div>

  <button class="btn-new-game" (click)="onNewGame()">
    NOVO JOGO
  </button>

  <p class="count-hint">{{ availableCount() }} LETRAS DISPONÍVEIS</p>
</div>
```

- [ ] **Step 3: Criar o CSS com animação do logo**

```css
/* src/app/components/home/home.component.css */
.home {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 24px;
  padding: 40px 20px;
  animation: fadeIn 0.5s ease;
}

@keyframes fadeIn {
  from { opacity: 0; transform: translateY(16px); }
  to   { opacity: 1; transform: translateY(0); }
}

.logo-wrapper {
  filter: drop-shadow(0 0 16px #00f5ff55);
}

.outer-ring {
  transform-origin: 65px 65px;
  animation: spin 12s linear infinite;
}

@keyframes spin {
  from { transform: rotate(0deg); }
  to   { transform: rotate(360deg); }
}

.logo {
  animation: pulse-logo 3s ease-in-out infinite;
}

@keyframes pulse-logo {
  0%, 100% { filter: drop-shadow(0 0 8px #00f5ff44); }
  50%       { filter: drop-shadow(0 0 20px #00f5ffaa); }
}

.title-block {
  text-align: center;
}

.title {
  font-size: 2.4rem;
  letter-spacing: 10px;
  color: var(--neon-cyan);
  text-shadow: var(--glow-cyan);
  font-family: monospace;
}

.subtitle {
  font-size: 0.7rem;
  letter-spacing: 3px;
  color: rgba(255, 255, 255, 0.3);
  margin-top: 4px;
}

.btn-new-game {
  padding: 14px 40px;
  border: 2px solid var(--neon-cyan);
  border-radius: 8px;
  background: transparent;
  color: var(--neon-cyan);
  font-family: monospace;
  font-size: 1rem;
  letter-spacing: 4px;
  cursor: pointer;
  text-shadow: var(--glow-cyan);
  box-shadow: var(--glow-cyan);
  transition: background 0.2s, box-shadow 0.2s;
}

.btn-new-game:hover {
  background: var(--neon-cyan-bg);
  box-shadow: 0 0 20px #00f5ffaa;
}

.btn-new-game:active {
  transform: scale(0.97);
}

.count-hint {
  font-size: 0.65rem;
  letter-spacing: 2px;
  color: rgba(255, 255, 255, 0.18);
}
```

- [ ] **Step 4: Verificar visualmente**

```bash
npx ng serve --open
```

Esperado: tela inicial neon com logo animado, título "STOP" e botão "NOVO JOGO". Clicar no botão deve mostrar "GAME STUB". Encerrar com `Ctrl+C`.

- [ ] **Step 5: Commit**

```bash
git add src/app/components/home/
git commit -m "feat: implement HomeComponent with animated SVG logo

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"
```

---

## Task 8: RouletteComponent

**Files:**
- Create: `src/app/components/roulette/roulette.component.ts`
- Create: `src/app/components/roulette/roulette.component.html`
- Create: `src/app/components/roulette/roulette.component.css`

> Este é o componente mais complexo. A animação usa 3 fases encadeadas via Web Animations API.

**Lógica da animação (importante para entender o código):**
- A janela do tambor tem 180px de altura (3 slots × 60px)
- Para o item `[i]` ficar centralizado na janela: `translateY = SLOT_HEIGHT - i * SLOT_HEIGHT = (1 - i) * 60`
- Início da animação: item[0] centralizado → `translateY = 60px`
- Fim da animação: item[n-1] (target) centralizado → `translateY = (1 - (n-1)) * 60 = (2-n) * 60`
- Com n=19: `translateY = (2-19)*60 = -1020px`

- [ ] **Step 1: Criar o TypeScript do RouletteComponent**

```typescript
// src/app/components/roulette/roulette.component.ts
import {
  Component, input, inject, effect,
  ElementRef, ViewChild, OnDestroy
} from '@angular/core';
import { GameService, GamePhase } from '../../services/game.service';
import { AudioService } from '../../services/audio.service';

const SLOT_HEIGHT    = 60;  // px por letra no tambor
const SEQUENCE_COUNT = 18;  // letras aleatórias antes da letra-alvo

@Component({
  selector: 'app-roulette',
  standalone: true,
  imports: [],
  templateUrl: './roulette.component.html',
  styleUrl: './roulette.component.css'
})
export class RouletteComponent implements OnDestroy {
  phase         = input<GamePhase>('idle');
  currentLetter = input<string | null>(null);

  @ViewChild('drum') drumRef!: ElementRef<HTMLElement>;

  /** Lista de letras renderizada no tambor durante/após a animação */
  displayLetters: string[] = ['A'];

  private tickTimeouts: ReturnType<typeof setTimeout>[] = [];
  private gameService  = inject(GameService);
  private audioService = inject(AudioService);

  constructor() {
    effect(() => {
      const p      = this.phase();
      const letter = this.currentLetter();
      if (p === 'spinning' && letter) {
        this.runAnimation(letter);
      }
      if (p === 'idle') {
        this.clearTickTimeouts();
      }
    });
  }

  // ─── Helpers ────────────────────────────────────────────────────────────

  /** Constrói sequência aleatória terminando com a letra-alvo */
  private buildSequence(target: string): string[] {
    const pool = 'ABCDEFGHIJKLMNOPQRSTUVWXYZ'.split('').filter(l => l !== target);
    const seq: string[] = [];
    for (let i = 0; i < SEQUENCE_COUNT; i++) {
      seq.push(pool[Math.floor(Math.random() * pool.length)]);
    }
    seq.push(target);
    return seq;
  }

  /** Agenda ticks de áudio sincronizados com as fases da animação */
  private scheduleTicks(accelMs: number, linearMs: number, decelMs: number): void {
    const phase12End = accelMs + linearMs;

    // Fases 1+2: tick a cada 125ms
    for (let t = 125; t < phase12End; t += 125) {
      this.tickTimeouts.push(setTimeout(() => this.audioService.playTick(), t));
    }

    // Fase 3: ticks com intervalos crescentes (desaceleração sonora)
    const decelIntervals = [125, 175, 250, 350, 480];
    let t = phase12End;
    for (const interval of decelIntervals) {
      t += interval;
      if (t < phase12End + decelMs) {
        const captured = t;
        this.tickTimeouts.push(setTimeout(() => this.audioService.playTick(), captured));
      }
    }

    // Ding na parada
    this.tickTimeouts.push(
      setTimeout(() => this.audioService.playDing(), phase12End + decelMs)
    );
  }

  private clearTickTimeouts(): void {
    this.tickTimeouts.forEach(id => clearTimeout(id));
    this.tickTimeouts = [];
  }

  /** Pulso de brilho no slot central após a parada */
  private async pulseGlow(): Promise<void> {
    const glowEl = this.drumRef.nativeElement
      .closest('.drum-window')
      ?.querySelector('.center-glow') as HTMLElement | null;
    if (!glowEl) return;
    await glowEl.animate([
      { opacity: '0', boxShadow: 'none' },
      { opacity: '1', boxShadow: 'inset 0 0 24px #00f5ff88, 0 0 20px #00f5ff66' },
      { opacity: '0.4', boxShadow: 'inset 0 0 8px #00f5ff44' },
      { opacity: '1', boxShadow: 'inset 0 0 24px #00f5ff88, 0 0 20px #00f5ff66' },
      { opacity: '0', boxShadow: 'none' }
    ], { duration: 600, easing: 'ease-in-out' }).finished;
  }

  // ─── Animação principal ──────────────────────────────────────────────────

  private async runAnimation(target: string): Promise<void> {
    const sequence = this.buildSequence(target);
    this.displayLetters = sequence;

    // Aguarda um frame para o Angular renderizar a nova lista
    await new Promise<void>(resolve => setTimeout(resolve, 16));

    if (!this.drumRef?.nativeElement) return;
    const drum = this.drumRef.nativeElement;

    // Posições translateY
    // Item[i] centralizado quando translateY = (1 - i) * SLOT_HEIGHT
    const n       = sequence.length;           // 19
    const startY  = SLOT_HEIGHT;               // item[0] centralizado: 60px
    const endY    = (2 - n) * SLOT_HEIGHT;     // item[n-1] centralizado: -1020px

    const p1Delta = SLOT_HEIGHT * 3;           // 3 letras na aceleração: 180px
    const p3Delta = SLOT_HEIGHT * 3;           // 3 letras na desaceleração: 180px

    const p1Start = startY;
    const p1End   = startY - p1Delta;
    const p2End   = endY   + p3Delta;
    const p3End   = endY;

    drum.style.transform = `translateY(${startY}px)`;

    this.clearTickTimeouts();
    this.scheduleTicks(400, 1500, 800);

    // Fase 1 — Aceleração (400ms)
    await drum.animate(
      [{ transform: `translateY(${p1Start}px)` }, { transform: `translateY(${p1End}px)` }],
      { duration: 400, easing: 'ease-in', fill: 'forwards' }
    ).finished;

    // Fase 2 — Rolagem constante (1500ms)
    await drum.animate(
      [{ transform: `translateY(${p1End}px)` }, { transform: `translateY(${p2End}px)` }],
      { duration: 1500, easing: 'linear', fill: 'forwards' }
    ).finished;

    // Fase 3 — Desaceleração (800ms)
    await drum.animate(
      [{ transform: `translateY(${p2End}px)` }, { transform: `translateY(${p3End}px)` }],
      { duration: 800, easing: 'cubic-bezier(0.25, 0.1, 0.1, 1.0)', fill: 'forwards' }
    ).finished;

    // Pulso de brilho
    await this.pulseGlow();

    // Notifica o GameService — mas só se ainda estiver em spinning
    // (resetGame pode ter sido chamado enquanto a animação rodava)
    if (this.phase() === 'spinning') {
      this.gameService.onAnimationComplete();
    }
  }

  ngOnDestroy(): void {
    this.clearTickTimeouts();
  }
}
```

- [ ] **Step 2: Criar o template HTML**

```html
<!-- src/app/components/roulette/roulette.component.html -->
<div class="drum-wrapper">
  <div class="drum-window">
    <!-- Fade top/bottom -->
    <div class="fade-overlay"></div>

    <!-- Linha de seleção central -->
    <div class="center-line top"></div>
    <div class="center-line bottom"></div>

    <!-- Glow overlay pulsante ao parar -->
    <div class="center-glow"></div>

    <!-- Tambor animado -->
    <div class="drum" #drum>
      @for (letter of displayLetters; track $index) {
        <div class="drum-slot">{{ letter }}</div>
      }
    </div>
  </div>
</div>
```

- [ ] **Step 3: Criar o CSS**

```css
/* src/app/components/roulette/roulette.component.css */
.drum-wrapper {
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 8px;
}

.drum-window {
  position: relative;
  width: 90px;
  height: 180px;          /* 3 slots × 60px */
  overflow: hidden;
  border: 2px solid var(--neon-cyan-dim);
  border-radius: 12px;
  background: var(--bg-secondary);
  box-shadow: var(--glow-cyan), inset 0 0 20px #00f5ff0a;
}

/* Fade nos topo e base */
.fade-overlay {
  position: absolute;
  inset: 0;
  background: linear-gradient(
    to bottom,
    var(--bg-secondary) 0%,
    transparent 28%,
    transparent 72%,
    var(--bg-secondary) 100%
  );
  z-index: 2;
  pointer-events: none;
}

/* Linhas de seleção do slot central */
.center-line {
  position: absolute;
  left: 8px;
  right: 8px;
  height: 1px;
  background: var(--neon-cyan-dim);
  z-index: 3;
  pointer-events: none;
}
.center-line.top    { top: 59px; }
.center-line.bottom { top: 119px; }

/* Glow overlay (animado via Web Animations API) */
.center-glow {
  position: absolute;
  top: 60px;
  left: 0;
  right: 0;
  height: 60px;
  opacity: 0;
  z-index: 4;
  pointer-events: none;
  border-top: 1px solid var(--neon-cyan-dim);
  border-bottom: 1px solid var(--neon-cyan-dim);
}

/* Drum container */
.drum {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  display: flex;
  flex-direction: column;
  will-change: transform;
  transform: translateY(60px); /* item[0] centralizado inicialmente */
}

.drum-slot {
  height: 60px;
  display: flex;
  align-items: center;
  justify-content: center;
  font-family: monospace;
  font-size: 2rem;
  font-weight: bold;
  color: var(--neon-cyan);
  text-shadow: var(--glow-cyan);
  letter-spacing: 2px;
  user-select: none;
}
```

- [ ] **Step 4: Commit**

```bash
git add src/app/components/roulette/
git commit -m "feat: implement RouletteComponent with 3-phase slot machine animation

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"
```

---

## Task 9: GameComponent — Orquestração Final

**Files:**
- Modify: `src/app/components/game/game.component.ts`
- Create: `src/app/components/game/game.component.html`
- Create: `src/app/components/game/game.component.css`

- [ ] **Step 1: Atualizar o TypeScript do GameComponent**

```typescript
// src/app/components/game/game.component.ts
import { Component, inject, output, computed } from '@angular/core';
import { GameService } from '../../services/game.service';
import { AudioService } from '../../services/audio.service';
import { RouletteComponent } from '../roulette/roulette.component';
import { AlphabetGridComponent } from '../alphabet-grid/alphabet-grid.component';

@Component({
  selector: 'app-game',
  standalone: true,
  imports: [RouletteComponent, AlphabetGridComponent],
  templateUrl: './game.component.html',
  styleUrl: './game.component.css'
})
export class GameComponent {
  goHome = output<void>();

  protected gameService = inject(GameService);
  private audioService  = inject(AudioService);

  /** Botão "Sortear" desabilitado fora do estado idle ou sem letras */
  canDraw = computed(() =>
    this.gameService.phase() === 'idle' &&
    this.gameService.availableLetters().length > 0
  );

  allDrawn = computed(() => this.gameService.availableLetters().length === 0);

  onDraw(): void {
    if (!this.canDraw()) return;
    this.gameService.drawLetter();
  }

  onNewGame(): void {
    this.audioService.ensureContext();
    this.gameService.resetGame();
    this.audioService.playSwoosh();
    this.goHome.emit();
  }
}
```

- [ ] **Step 2: Criar o template HTML**

```html
<!-- src/app/components/game/game.component.html -->
<div class="game">

  <app-roulette
    [phase]="gameService.phase()"
    [currentLetter]="gameService.currentLetter()"
  />

  @if (allDrawn()) {
    <p class="all-drawn-msg">✓ Todas as letras foram sorteadas!</p>
  } @else {
    <button class="btn-draw" [disabled]="!canDraw()" (click)="onDraw()">
      ▶ SORTEAR
    </button>
  }

  <app-alphabet-grid [drawnLetters]="gameService.drawnLetters()" />

  <button class="btn-reset" (click)="onNewGame()">
    ↺ Novo Jogo
  </button>

</div>
```

- [ ] **Step 3: Criar o CSS**

```css
/* src/app/components/game/game.component.css */
.game {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 20px;
  padding: 32px 20px;
  animation: fadeIn 0.4s ease;
}

@keyframes fadeIn {
  from { opacity: 0; transform: translateY(12px); }
  to   { opacity: 1; transform: translateY(0); }
}

.btn-draw {
  padding: 12px 32px;
  border: 2px solid var(--neon-purple);
  border-radius: 8px;
  background: transparent;
  color: var(--neon-purple);
  font-family: monospace;
  font-size: 1rem;
  letter-spacing: 3px;
  cursor: pointer;
  text-shadow: var(--glow-purple);
  box-shadow: var(--glow-purple);
  transition: background 0.2s, box-shadow 0.2s, opacity 0.2s;
}

.btn-draw:hover:not(:disabled) {
  background: #bf00ff0a;
  box-shadow: 0 0 20px #bf00ffaa;
}

.btn-draw:active:not(:disabled) {
  transform: scale(0.97);
}

.btn-draw:disabled {
  opacity: 0.35;
  cursor: not-allowed;
  box-shadow: none;
}

.all-drawn-msg {
  font-family: monospace;
  font-size: 0.8rem;
  letter-spacing: 2px;
  color: var(--neon-cyan);
  text-shadow: var(--glow-cyan);
  padding: 10px 20px;
  border: 1px solid var(--neon-cyan-dim);
  border-radius: 8px;
  background: var(--neon-cyan-bg);
}

.btn-reset {
  padding: 7px 18px;
  border: 1px solid rgba(255, 255, 255, 0.15);
  border-radius: 6px;
  background: transparent;
  color: rgba(255, 255, 255, 0.3);
  font-family: monospace;
  font-size: 0.75rem;
  letter-spacing: 2px;
  cursor: pointer;
  transition: border-color 0.2s, color 0.2s;
}

.btn-reset:hover {
  border-color: rgba(255, 255, 255, 0.35);
  color: rgba(255, 255, 255, 0.6);
}
```

- [ ] **Step 4: Verificar integração completa**

```bash
npx ng serve --open
```

Testar manualmente:
1. Clicar "NOVO JOGO" → chega na tela de jogo
2. Clicar "▶ SORTEAR" → tambor anima, letra sorteada aparece, grade atualiza
3. A letra sorteada fica riscada na grade
4. Clicar "SORTEAR" durante animação → botão desabilitado, nada acontece
5. Clicar "↺ Novo Jogo" → volta para a tela inicial

Encerrar com `Ctrl+C`.

- [ ] **Step 5: Rodar todos os testes para confirmar que nada quebrou**

```bash
npx ng test --watch=false --browsers=ChromeHeadless
```

Esperado: todos os specs passando, 0 falhas.

- [ ] **Step 6: Commit**

```bash
git add src/app/components/game/
git commit -m "feat: implement GameComponent wiring all pieces together

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"
```

---

## Task 10: Polimento Final

**Files:**
- Modify: `src/app/app.component.css`
- Modify: `src/styles.css`

- [ ] **Step 1: Garantir responsividade mobile em `styles.css`**

Adicionar ao final de `src/styles.css`:

```css
/* Responsividade */
@media (max-width: 400px) {
  :root {
    font-size: 14px;
  }
}

/* Scrollbar discreta */
::-webkit-scrollbar {
  width: 4px;
}
::-webkit-scrollbar-track {
  background: var(--bg-primary);
}
::-webkit-scrollbar-thumb {
  background: var(--neon-cyan-dim);
  border-radius: 2px;
}
```

- [ ] **Step 2: Adicionar `.gitignore` adequado se não existir**

Verificar se `.gitignore` já contém `.superpowers/`. Se não:

```bash
echo ".superpowers/" >> /c/Users/Douglas/source/pessoais/jogo-stop/.gitignore
```

- [ ] **Step 3: Rodar teste final completo**

```bash
npx ng test --watch=false --browsers=ChromeHeadless
npx ng build
```

Esperado: todos os specs passando, build bem-sucedido sem erros.

- [ ] **Step 4: Commit final**

```bash
git add src/styles.css .gitignore
git commit -m "feat: add responsive polish and production build verification

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"
```

---

## Resumo das Tasks

| # | Task | Entrega |
|---|---|---|
| 1 | Bootstrap Angular | Projeto rodando com `ng serve` |
| 2 | Tema CSS | Variáveis neon/dark aplicadas |
| 3 | GameService | 17 specs verdes |
| 4 | AudioService | 5 specs verdes, sons procedurais |
| 5 | AlphabetGridComponent | 5 specs verdes, grade A–Z |
| 6 | AppComponent shell | Navegação home ↔ game |
| 7 | HomeComponent | Logo SVG animado + botão |
| 8 | RouletteComponent | Animação 3 fases + áudio |
| 9 | GameComponent | App completo e integrado |
| 10 | Polimento | Build de produção limpo |
